{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "init-project",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}/src"
    }
  ]
}
